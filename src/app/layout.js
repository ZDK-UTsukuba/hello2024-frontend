export const metadata = {
  title: "筑波大学 新入生情報Web 2024",
  description: "筑波大学 全学学類・専門学群・総合学域群代表者会議",
};

const Layout = ({ children }) => {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
};

export default Layout;
